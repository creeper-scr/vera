# Records index

Dated design / plan notes. **Not** the live contract — prefer [`docs/handbook/`](../handbook/) for current ownership boundaries. Code remains source of truth.

## Active companion work

| Doc | Notes |
|-----|--------|
| [`handbook/game-companion.md`](../handbook/game-companion.md) | Stable layering (voice / decision / adapter) |

## Historical / other plans

Files under `assistant/`, `design/`, `plans/`, `requirements/`, `solutions/` may describe surfaces this fork no longer maintains (desktop nightly, in-repo auth UI, official provider login). Treat them as archive unless a handbook page still points at them.

List what is on disk under `docs/records/` rather than assuming this index is exhaustive after cuts.
